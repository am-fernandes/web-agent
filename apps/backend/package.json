{
  "name": "@web-agent/backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python backend built'",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "clean": "rm -rf tmp/ __pycache__/ .pytest_cache/"
  }
}